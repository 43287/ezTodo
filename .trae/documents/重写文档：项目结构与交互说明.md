## 目标
- 删除现有 `doc/` 目录中的旧文档；新增一份权威文档《项目结构与交互说明》，聚焦：前后端交互、用户如何通过输入/输出操作数据（任务与计划）。

## 新文档结构（doc/项目结构与交互说明.md）
- 概览
  - 技术栈与运行形态：Next 16 + React 19 + Tailwind v4（静态导出），Tauri 2 + Rust 本地后端
  - 构建流程：先构建前端再打包 Tauri（`src-tauri/tauri.conf.json:7-11`）
- 目录结构
  - 根目录、`src-tauri/`（后端）、`otherP/`（前端）；关键文件路径与职责
  - 代码引用：
    - 后端命令注册与初始化：`src-tauri/src/lib.rs:16-28`
    - Todo 命令：`src-tauri/src/api/todo.rs:52-95`
    - Plan 命令：`src-tauri/src/api/plan.rs:112-164`
    - 前端调用封装：`otherP/lib/tauriClient.ts`
    - 前端状态与交互：`otherP/context/todo-context.tsx`
- 数据模型
  - Todo 与 Plan 的字段说明（对齐前端类型），示例对象
- 后端命令 API
  - Todo：`todo_create`、`todo_list`、`todo_get`、`todo_update`、`todo_delete`
  - Plan：`plan_create`、`plan_list`、`plan_update`、`plan_delete`、`plan_refresh`
  - 请求/响应示例（JSON）与错误码约定（BAD_INPUT/NOT_FOUND/IO_ERROR/LOCK_ERROR）
- 前端交互流程
  - 初始化：并发加载 `todoList` + `planList`，随后 `planRefresh`（`otherP/context/todo-context.tsx:224-234`）
  - 用户操作映射：
    - 添加任务 → 调用 `todoCreate`，更新 UI 与历史
    - 更新/完成/重要切换 → 调用 `todoUpdate`
    - 删除任务 → 调用 `todoDelete`
    - 添加计划 → 调用 `planCreate`；更新历史
    - 更新/激活/重要切换 → 调用 `planUpdate`
    - 删除计划 → 调用 `planDelete`；清理关联任务
    - 周期刷新 → `planRefresh` 自动/手动触发
  - 具体代码段与调用示例
- 用户操作指南（输入/输出）
  - 输入：表单字段说明、必填规则、校验反馈（前端 `zod`）
  - 输出：列表、状态标记、历史记录变化；完成后 UI 变化说明
  - 常见操作：新增、编辑、完成、删除、计划启动/暂停、结束
- 构建与运行
  - 开发：`pnpm -C otherP dev` + `pnpm tauri dev`
  - 打包：`pnpm tauri build`（自动构建前端并读取 `frontendDist`）
- 安全与性能
  - 输入校验、持久化原子写、锁并发；复杂度与优化建议（索引、批处理）

## 执行步骤
1. 删除 `doc/` 下所有旧 `.md` 文件。
2. 新增 `doc/项目结构与交互说明.md`，按上述结构写入完整内容，包含代码引用与示例。
3. 自查链接与引用是否正确，保证一份文档即可满足需求。

## 交付
- 完成后提供文档路径与主要小节速览，便于快速导航与查阅。